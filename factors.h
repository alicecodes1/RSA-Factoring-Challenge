#ifndef FACTORS_H
#define FACTORS_H

#include <vector>
#include <string>

std::vector<std::pair<int, int>> factorize(int number);
void factorize_file(const std::string &file_path);

#endif /* FACTORS_H */
